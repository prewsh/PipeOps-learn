PIPEOPS / UGC PROGRAMME

{{ if eq .RedirectTo "https://pipeops-learn.pipeops.app/login" }}YOU'RE INVITED TO PIPEOPS LEARN

You're invited to join PipeOps Learn, where you'll get access to your learning
modules and updates about the PipeOps UGC Programme.

Sign in at https://pipeops-learn.pipeops.app/login using your accepted email
address. We'll send you a separate sign-in email with a code and link.

You're receiving this at {{ .Email }} because you've been accepted into the
PipeOps UGC Programme.
{{ else }}SIGN IN

Enter this code on the sign-in screen. It works once, and expires in an hour.

    {{ .Token }}

Or tap here to sign in:
{{ if or (eq .RedirectTo "https://pipeops-learn.pipeops.app/auth/confirm") (eq .RedirectTo "http://localhost:3000/auth/confirm") }}{{ .RedirectTo }}{{ else }}https://pipeops-learn.pipeops.app/auth/confirm{{ end }}?token_hash={{ .TokenHash }}&type=magiclink
If the link doesn't work in your browser, use the code above.

This programme is invite-only and this code was sent to {{ .Email }}. If you
weren't expecting it, ignore this email - nothing happens until the code is used.
{{ end }}
